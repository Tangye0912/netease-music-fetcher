# music-fetch v0.5.0 发布说明

发布日期：2026-03-26  
版本代号：批量下载稳定版

## 一、版本重点

`v0.5.0` 聚焦“批量下载全流程可用性”，核心目标是：  
- 让用户可以直接粘贴混合链接（单曲/歌单/分享文案）并完成批量下载；  
- 让批量下载过程可中断、可调整参数、可继续，不再重复做无效操作。

## 二、新增功能

### 1) 批量识别与下载主流程

- 入口：主界面输入框粘贴多个链接后，点击 `检测`
- 行为：
  - 自动解析多条候选输入
  - 自动识别歌单链接并展开歌曲
  - 结果按 `song_id` 去重
  - 识别结果展示来源、歌曲 ID、歌曲名、资源大小、状态

### 2) 歌单分享链接识别

- 入口：主界面输入框 / 批量输入框
- 支持：
  - `playlist` 链接直接识别
  - 贴入分享文案自动提取歌单链接
- 结果：
  - 展示为可勾选下载的歌曲列表

### 3) 批量并发下载

- 入口：批量页 `下载选中项`
- 行为：
  - 支持按并发上限同时下载多个任务
  - 实时显示“已完成/总数/并发中任务数”
  - 支持批量取消

### 4) 批量页内下载设置

- 入口：批量页底部按钮 `下载设置`
- 可调整：
  - 检测超时
  - 下载超时
  - 下载重试次数
  - 并发上限
- 关键行为：
  - 可在批量流程中直接改参数，不用退回主界面
  - 改完可继续当前批次剩余下载

### 5) 批量识别结果缓存恢复

- 入口：同一批输入再次进入批量页
- 行为：
  - 若输入内容未变化，自动恢复上次识别结果
  - 无需重新跑识别，减少重复等待

## 三、界面与交互优化

### 1) 主界面底部信息

- 原：`状态：待输入`
- 新：
  - `版本：x.y.z`（可点击检查更新）
  - `GitHub 项目`（可点击跳转仓库）

### 2) 下载管理筛选区

- 筛选下拉框宽度加宽，选项文案完整展示
- 筛选无结果时布局不再抖动（筛选栏保持原位）

### 3) 软件设置页面

- “下载设置”区域去掉背景分组块，布局更简洁
- 各下拉框统一宽度与对齐
- 超时选项调整为常用档位：
  - 检测超时：`1s / 3s / 5s`
  - 下载超时：`3s / 5s / 10s`

### 4) 批量结果表格

- 表头宽度改为稳定固定布局
- 全选/反选后不再改变列宽

## 四、行为变更（请注意）

1. 批量下载默认支持并发执行（并发值取自“软件设置”或批量页“下载设置”）。  
2. 批量流程取消后可调整并发/超时参数并继续下载剩余项。  
3. 同一输入可复用已解析结果，减少重复识别开销。  
4. 超时设置范围改为固定常用档位（不再提供大范围秒数列表）。

## 五、操作路径速查

- 主流程检测：主界面 -> 输入链接 -> `检测`
- 批量设置：批量页 -> `下载设置`
- 批量下载：批量页 -> 勾选歌曲 -> `下载选中项`
- 取消下载：批量页 -> `取消下载`
- 下载筛选：下载管理 -> `状态筛选`
- 版本检查：主界面底部 -> 点击 `版本：x.y.z`

## 六、测试与稳定性

- 单元测试：`python3.13 -m unittest discover -s tests`（63 通过）
- 语法检查：`python3.13 -m py_compile main.py ui_texts.py app_settings.py`

