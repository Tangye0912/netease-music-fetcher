# Roadmap

## v0.4.0 (Completed - 2026-03-23)

### Goal

- 把当前单曲下载流程整理为可扩展的“任务中心”基础，为 `v0.5.0` 批量下载做铺垫。已完成。

### Scope

- [x] 下载任务状态模型统一：`pending/downloading/success/failed/canceled`
- [x] GUI 主流程接入任务状态生命周期（单任务）
- [x] 下载管理支持按任务状态筛选
- [x] 单任务失败重试入口
- [x] 下载参数扩展：超时/重试次数/并发上限（并发默认仍为 1）
- [x] 任务级日志追踪（按 task_id）

### Engineering Rules

- 每条功能改动必须补对应测试（单元测试或流程测试）。
- 每条功能改动必须补对应文档/记录（`CHANGELOG`、`README` 或本文件）。
- 新增关键逻辑需加简洁注释并注明版本来源（例如 `v0.4.0`）。
- 发现未使用代码时，优先删除；若需要保留历史兼容，再显式注释原因与计划移除版本。

## v0.5.0 (Planned)

### Goal

- 提供稳定的批量下载体验（多任务入队、并发下载、批次结果可追踪）。

### Scope

- 批量输入（多行粘贴/文本导入）与预检。
- 任务列表与总进度统计（总数/成功/失败/进行中）。
- 并发调度（可配置并发数）。
- 批量操作（开始、取消、重试失败）。
- 批次结果汇总（失败原因分组）。
